package sockets

import (
	/*	"github.com/kazoup/platform/notification/web/sockets"
		"net/http"
		"net/http/httptest"*/
	"testing"
)

func TestPingPlatform(t *testing.T) {
	/*
		r := httptest.NewRecorder()

		http.Handle()
		handler := http.HandlerFunc(PingPlatform())
		handler.ServeHTTP(r, req)

		web.Handle("/platform/ping", websocket.Handler(PingPlatform))
		web.HandleFunc("/health", handler.HandleHealthCheck)

		websocket.
	*/

}
