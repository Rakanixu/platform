apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: db-srv
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: db-srv
    spec:
      containers:
      - name: db
        command: [
                  "/srv",
                  "--registry=consul",
                  "--registry_address=$(REGISTRY_SERVICE_HOST)",
                  "--server_address=0.0.0.0:9090",
                  "--broker=nats",
                  "--broker_address=nats:4222",
                  "--selector=cache",
                  "--transport=tcp",
                  ]
        image: eu.gcr.io/desktop-1470249894548/db-srv:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 9090
          name: db-port
        - containerPort: 10001
          name: broker-port
