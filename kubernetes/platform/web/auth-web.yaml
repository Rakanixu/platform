apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: auth-web
spec:
  replicas: 1
  revisionHistoryLimit: 3
  template:
    metadata:
      labels:
        app: auth-web
    spec:
      containers:
      - name: auth-web
        command: [
                  "/web",
                  "--registry_address=$(REGISTRY_SERVICE_HOST)",
                  "--server_address=0.0.0.0:9090",
                  "--register_ttl=60",
                  "--register_interval=30",
                  "--broker=nats",
                  "--broker_address=nats:4222",
                  ]
        image: eu.gcr.io/desktop-1470249894548/auth-web:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 9090
          name: auth-port
